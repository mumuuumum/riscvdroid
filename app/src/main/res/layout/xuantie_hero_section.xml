<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Hero Section容器，包含自定义背景和内容 -->
    <FrameLayout
        android:id="@+id/hero_container"
        android:layout_width="match_parent"
        android:layout_height="60dp">

        <!-- 自定义渐变背景和几何图案 -->
        <org.fdroid.fdroid.views.hero.XuanTieHeroView
            android:id="@+id/hero_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Hero内容容器 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp">

            <!-- 标题/Logo -->
            <TextView
                android:id="@+id/hero_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="玄铁开源"
                android:textColor="@color/xuantie_hero_title"
                android:textSize="18sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif"
                android:shadowColor="#40000000"
                android:shadowDx="1"
                android:shadowDy="1"
                android:shadowRadius="2"
                tools:text="玄铁开源" />

            <!-- 搜索容器 -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="36dp"
                android:layout_weight="1"
                android:layout_marginStart="12dp"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:background="@drawable/search_input_background">

                <!-- 搜索输入框 -->
                <TextView
                    android:id="@+id/hero_search_input"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:layout_marginStart="12dp"
                    android:layout_marginEnd="8dp"
                    android:gravity="center_vertical"
                    android:hint="搜索应用"
                    android:textColorHint="#80FFFFFF"
                    android:textSize="14sp"
                    android:textColor="#FFFFFF"
                    android:background="@null"
                    android:focusable="true"
                    android:clickable="true"
                    tools:text="搜索应用" />

                <!-- 搜索按钮 -->
                <ImageButton
                    android:id="@+id/hero_search_button"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_marginEnd="4dp"
                    android:src="@drawable/ic_search"
                    android:background="@drawable/search_button_background"
                    android:contentDescription="搜索"
                    android:scaleType="centerInside"
                    android:tint="#FFFFFF" />

            </LinearLayout>

        </LinearLayout>

    </FrameLayout>

</LinearLayout>